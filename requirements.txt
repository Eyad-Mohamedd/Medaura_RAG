# App framework
fastapi==0.136.3
uvicorn==0.49.0
pydantic==2.13.4
python-dotenv==1.2.2

# LangChain stack
langchain==1.3.8
langchain-community==0.4.2
langchain-core==1.4.6
# OpenAI — used for BOTH the chat model (GPT-4.1) and embeddings
# (text-embedding-3-small). Loose pin within the 1.x line so it resolves
# against langchain-core 1.4.x; bump if pip can't find a compatible build.
langchain-openai>=1.0,<2
langchain-chroma==1.1.0

# Retrieval / vector store
# NOTE: embeddings are now served by the OpenAI API, so torch +
# sentence-transformers + langchain-huggingface are intentionally gone — that's
# ~2GB less to build/download and far less RAM at runtime.
chromadb==1.5.9
rank-bm25==0.2.2
